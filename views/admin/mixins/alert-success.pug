mixin alert-success(time)
    - if(messages.success)
        .messages.info 
            div(
                class="alert alert-success"
                show-alert
                data-time = time
            )
                div #{messages.success} 
                div(close-notifycation) <span>X</span> 